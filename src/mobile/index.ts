/**
 * railgun-reloaded/storage/mobile - React Native database adapter entry.
 *
 * TODO: not yet implemented. This entry will be the React Native counterpart
 * of `./node`: database lifecycle for a mobile SQLite driver (expo-sqlite or
 * op-sqlite — driver choice pending), a `Transactor` for that driver, and
 * one-argument factory wrappers that wire a mobile database into the
 * platform-neutral `ChainStorage` and `WalletStorage` contracts from the
 * root entry. The adapter must pass the same contract suite as the Node
 * adapter (`test/contract/`). Wire the `./mobile` subpath into the package
 * `exports` map when the adapter lands.
 */

export {}
